# ADAE Results

This directory stores generated artifacts and benchmark measurements.

Phase-partitioned layout:

- `prepare/`: outputs from the prepare stage
- `run/`: outputs from the run stage
- `aggregate/`: outputs from the aggregate stage

Subdirectories are created on demand by the ADAE scripts.

Generated files in this directory are intentionally ignored by git, except for this README and `.gitignore`.
