# 种子 QASM 用例

这些电路刻意保持很小，并尽量手写维护。

其目的有三点：

- 保持运行时间短，便于高频回归
- 在失败时将问题尽量收敛到单一语义路径
- 为不同 backend 提供稳定、可重复的最小验证集

当前已归档的典型覆盖包括：

- 单比特确定态
- 单比特叠加态
- 两比特纠缠支撑集
- `U3` 门路径
- `MPI` 最小跨 worker 交换路径

建议命名约定：

- `<gate_or_pattern>_<qubits>_<oracle>.qasm`
